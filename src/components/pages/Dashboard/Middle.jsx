import React from "react";

export default function Middle(props) {
  const { data } = props;
  return <div></div>;
}
/*

export default function today() {
  //loading today data
  return <Middle data={today}/>;
}

export default function tomorrow() {
  //loading in tomorrow data
  return <Middle data={tomorrow}/>;
}

export default function upcoming() {
  //loading in upcoming data
  return <Middle data={upcoming}/>;
}
*/
